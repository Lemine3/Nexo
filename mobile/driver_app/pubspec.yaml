name: meshily_driver
description: "Meshily Delivery — driver app | مشيلي للتوصيل — تطبيق السائق"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  socket_io_client: ^2.0.3+1
  flutter_map: ^6.2.1
  latlong2: ^0.9.1
  url_launcher: ^6.3.1
  geolocator: ^13.0.1
  image_picker: ^1.1.2
  # Push notifications — uncomment after adding google-services.json:
  # firebase_core: ^3.8.0
  # firebase_messaging: ^15.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
